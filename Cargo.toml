[workspace]
members = [
    ".",
    "profiler",
]

[package]
name = "sdf-edit-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
profiler = { path = "profiler" }

log = "*"
simple_logger = "*"

[features]
profiling = [
    "profiler/enabled",
]
